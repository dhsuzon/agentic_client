import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account - TutorialsPoint",
  description: "Join TutorialsPoint and start learning with AI-powered courses. Create your free account today.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
